/// operator aritmatika
/// +, -, *  dan / %
/// operator perbandingan
/// ==,!=, <, >, >=, <=
/// operator logika
/// &&, ||, !
/// operator penugasan
/// =, +=, -=, *=, /=, %=
/// operator ternary
void main(List<String> args) {
  int a = 5;
  int b = 3;
  final angkaKita;
  angkaKita = 10;
  print(angkaKita);
  int result = (a < b) ? a : b;
  print(result);
}
